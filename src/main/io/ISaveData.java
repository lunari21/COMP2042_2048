package main.io;

/**
 * Interface used to categorize savable classes
 * @author Alexander Tan Ka Jin
 */
public interface ISaveData {
	//Each save file has different variables and actually share no methods between them. So this is empty.
	//This is, despite the fact that they all only have one variable. The saving functionality of this project
	//is admittedly very rudimentary. I left these interfaces so that in the future, they can possibly be extended upon.
}
