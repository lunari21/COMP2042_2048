package main.io;

/**
 * Loader Interface that is mostly used to catergorize loaders.
 * @author Alexander Tan Ka Jin
 */
public interface ILoader {
	//Empty because it was crucial that each of the loadX() functions return different classes
}
